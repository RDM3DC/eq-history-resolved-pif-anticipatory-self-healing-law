# Stronger evidence block for TopEquations submission

Suggested evidence section:

- Builds directly on the history-resolved phase conductance law and the self-healing conductance law by combining them in one edge update.
- `xi = 0` recovers the history-resolved self-healing law without π_f anticipation.
- `chi = 0` isolates a pure π_f anticipatory routing / healing variant.
- `xi = chi = 0` recovers the ungated history-resolved conductance update.
- Graph-mode ablation artifacts are attached: executable benchmark script, CSV outputs, and comparison plots for π_f-on vs π_f-off across classic and maze modes.
- In the current graph-mode prototype, π_f-on improves final effective-current recovery in maze mode and sharply reduces tail loop-signature mismatch relative to π_f-off.
- The strongest present empirical signal is lower post-damage loop-mismatch residual under active π_f feedback; larger topology-level claims remain to be tested in the lattice solver.
- The anticipatory claim is falsifiable: if π_f loop mismatch does not separate or improve post-damage trajectories under matched ablations, the added channel fails.
